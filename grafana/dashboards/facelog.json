{
  "annotations": {"list": []},
  "editable": true,
  "gnetId": null,
  "graphTooltip": 0,
  "panels": [
    {
      "type": "timeseries",
      "title": "HTTP Request Latency (p50/p95)",
      "targets": [
        {"expr": "histogram_quantile(0.5, sum(rate(facelog_request_latency_seconds_bucket[5m])) by (le))"},
        {"expr": "histogram_quantile(0.95, sum(rate(facelog_request_latency_seconds_bucket[5m])) by (le))"}
      ],
      "gridPos": {"x": 0, "y": 0, "w": 12, "h": 8}
    },
    {
      "type": "timeseries",
      "title": "HTTP Requests per Second",
      "targets": [
        {"expr": "sum(rate(facelog_requests_total[1m])) by (endpoint)"}
      ],
      "gridPos": {"x": 12, "y": 0, "w": 12, "h": 8}
    }
  ],
  "schemaVersion": 36,
  "style": "dark",
  "tags": ["facelog"],
  "time": {"from": "now-6h", "to": "now"},
  "title": "Facelog Overview",
  "version": 1
}






